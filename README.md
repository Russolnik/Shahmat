# 🎮 Шашки - Telegram Mini App

Веб-приложение шашек, запускаемое как Telegram Mini App.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm run install:all
```

### Запуск в режиме разработки

```bash
npm run dev
```

Это запустит:
- Frontend на `http://localhost:5173`
- Backend на `http://localhost:3000`

## 📁 Структура проекта

```
├── frontend/          # React + Vite + Tailwind CSS
│   ├── src/
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── utils/
│   │   └── App.jsx
│   └── package.json
├── backend/           # Node.js + Express + Socket.IO
│   ├── server.js
│   ├── gameLogic.js
│   └── package.json
└── package.json
```

## 🎯 Особенности

- ✅ Адаптивная доска 8×8 (работает на экранах от 320px)
- ✅ Темная/светлая тема с переключением
- ✅ Плавные анимации ходов (GPU-ускорение)
- ✅ WebSocket для обновлений в реальном времени
- ✅ Авторизация через Telegram Mini App
- ✅ Оптимизация для слабых устройств (CSS Grid вместо Canvas)
- ✅ Полная логика шашек:
  - Простые фишки и дамки
  - Обязательные взятия
  - Цепочки взятий
  - Превращение в дамку
- ✅ Модальное окно с правилами игры
- ✅ Предложение ничьей
- ✅ Сдача

## 🔧 Технологии

- **Frontend**: React 18, Vite, Tailwind CSS
- **Backend**: Node.js, Express, Socket.IO
- **Интеграция**: Telegram Mini App API
- **Стилизация**: CSS Variables для тем, CSS Grid для доски

## 📋 Игровые правила

- Игра ведётся на доске 8×8, используются только тёмные клетки
- Белые фишки начинают игру
- Простая фишка ходит по диагонали вперёд на одну клетку
- Дамка ходит по диагонали на любое расстояние
- Взятие обязательно, если возможно
- Цепочки взятий продолжаются автоматически
- Фишка становится дамкой, достигнув противоположного края доски

Подробные правила доступны в игре через кнопку "Правила".

## 🛠 Разработка

### Структура проекта

```
├── frontend/
│   ├── src/
│   │   ├── components/      # React компоненты
│   │   │   ├── Board.jsx    # Игровая доска
│   │   │   ├── GameInfo.jsx # Информация об игре
│   │   │   ├── GameControls.jsx # Кнопки управления
│   │   │   └── RulesModal.jsx   # Модальное окно правил
│   │   ├── hooks/           # React хуки
│   │   │   ├── useTelegramAuth.js
│   │   │   ├── useGameSocket.js
│   │   │   └── useTheme.js
│   │   ├── App.jsx          # Главный компонент
│   │   └── index.css        # Глобальные стили
│   └── package.json
├── backend/
│   ├── server.js            # Express сервер + Socket.IO
│   ├── gameManager.js       # Управление играми
│   ├── checkersGame.js      # Логика игры
│   ├── checkersLogic.js     # Правила и валидация ходов
│   ├── auth.js              # Авторизация
│   └── package.json
└── package.json             # Корневой package.json
```

### API Endpoints

- `POST /api/auth` - Авторизация
- `POST /api/game/create` - Создать игру
- `POST /api/game/join/:id` - Присоединиться к игре
- `GET /api/game/:id/state` - Получить состояние игры
- `WebSocket /ws` - Обновления в реальном времени

### WebSocket Events

**Клиент → Сервер:**
- `joinGame(gameId, userId)` - Присоединиться к игре
- `getPossibleMoves({ row, col })` - Получить возможные ходы
- `makeMove({ from, to })` - Сделать ход
- `surrender` - Сдаться
- `offerDraw` - Предложить ничью
- `acceptDraw` - Принять ничью
- `rejectDraw` - Отклонить ничью

**Сервер → Клиент:**
- `gameState` - Обновление состояния игры
- `moveResult` - Результат хода
- `drawOffered` - Предложение ничьей
- `drawRejected` - Отклонение ничьей

## 📱 Развёртывание

Подробные инструкции по развёртыванию см. в [DEPLOY.md](./DEPLOY.md)

## ⚠️ Важно

В режиме разработки авторизация упрощена для удобства тестирования. В продакшене обязательно реализуйте валидацию `initData` через секретный ключ Telegram!

## 📄 Лицензия

MIT

